# Project Instructions

## Git

- Never mention Claude, AI, or any AI assistant in commits, code, or comments
- Never use `git add .` or `git add --all` - add each file intentionally
- Commit messages must be minimal one-liners (no body, no multi-line)
- Do not include "Co-Authored-By" lines
