#!/bin/sh

powered=$(bluetoothctl show | awk '/Powered:/ {print $2}')
[ -z "$powered" ] && { notify-send "Bluetooth" "No adapter found"; exit 1; }
[ "$powered" = yes ] || bluetoothctl power on
bluetoothctl connect 14:3F:A6:3D:71:E3
