#!/bin/sh

## Blacklist i2c_hid in its own modprobe.d file, then offer to reboot.
## Needs sudo!

set -e

printf 'blacklist i2c_hid\n' | sudo -A tee /etc/modprobe.d/i2c_hid.conf >/dev/null

prompt "Reboot now?" "sudo -A reboot"
