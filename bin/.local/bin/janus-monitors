#!/bin/sh
# Toggle HDMI-1 on/off for when the right monitor switches to Noctua
# When HDMI-1 is active: "HDMI-1 connected 2560x1440+..."
# When HDMI-1 is off:    "HDMI-1 connected (normal..."

if xrandr --query | grep "^HDMI-1" | grep -q "[0-9]x[0-9]"; then
  xrandr --output HDMI-1 --off
else
  xrandr --output HDMI-1 --off
  xrandr --output DP-2 --mode 2560x1440 --rotate left --pos 0x0
  xrandr --output HDMI-1 --mode 2560x1440 --rotate normal --right-of DP-2
fi
